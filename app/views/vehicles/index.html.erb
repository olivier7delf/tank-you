<div class="container">

  <div class="d-flex align-items-center search-form search-filter">
    <form action="/vehicles" method="GET" class="d-flex align-items-center search-form search-filter">
      <input type="date" name="start_date" placeholder="<%= Date.today %>" >
      <input type="date" name="end_date" placeholder="end date: <%= Date.today+1 %>" >
      <input type="integer" name="max_capacity" placeholder="max invités: <%= params[:max_capacity] || 10000 %>" >
      <input type="integer" name="min_price" placeholder="min: <%= params[:min_price] || 1 %> €" >
      <input type="integer" name="max_price" placeholder="max: <%= params[:max_price] || 100000 %> €" >
      <select id="category" name="category" >
        <option value="<%= params[:category] %>"><%= params[:category] %></option>
        <% Vehicle::CATEGORIES.each do |category| %>
          <% unless category == params[:category] %>
            <option value="<%= category %>"><%= category %></option>
          <% end %>
        <% end %>
      </select>
      <button type="submit" class="search-btn">
        <i class="fas fa-search"></i>
      </button>
    </form>
  </div>

  </div>
  <div class="cards">
    <% @vehicles.each do |vehicle| %>
      <div class="property-card">
        <%= link_to vehicle_path(vehicle) do %>
          <div class="property-image" style="background-image: url('<%= cl_image_path vehicle.photos[0].key %>')">
            <div class="property-image-title">
              <!-- Optional <h5>Card Title</h5> If you want it, turn on the CSS also. -->
            </div>
          </div>

          <div class="property-description">
            <h5> <%= vehicle.name %> </h5>
            <p><%= vehicle.category %></p>
            <p>Up to <%= vehicle.capacity %> people, <%= vehicle.daily_price %> €</p>
          </div>
        <% end %>

          <div class="property-social-icons", style="background-color:white">
            <%= link_to "🚀", new_vehicle_order_path(vehicle) %>
            <!-- I would usually put multipe divs inside here set to flex. Some people might use Ul li. Multiple Solutions -->
          </div>

      </div>
    <% end %>
  </div>
</div>
